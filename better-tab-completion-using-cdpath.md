## Better tab completion in shell

`CDPATH` is the environment variable that holds the paths to search for when you press `TAB`. Add your most visited directories to it for a better tab completion.

Example:
`export CDPATH=".:~/code:~"`